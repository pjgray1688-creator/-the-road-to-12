import type { Exercise } from "./types";
import { loadIncrement, nextPracticalLoad } from "./coach";
export type ProgressionProfile = { kind: "dumbbell" | "barbell" | "selectorised_machine" | "plate_loaded_machine"; baseIncrement: number; rampStyle: "proportional" | "plate_jump" | "small_step" };
export function progressionProfile(exercise: Exercise): ProgressionProfile { if (exercise.equipment === "dumbbell") return { kind: "dumbbell", baseIncrement: 2, rampStyle: "small_step" }; if (exercise.equipment === "barbell") return { kind: "barbell", baseIncrement: 2.5, rampStyle: "proportional" }; if (exercise.id === "leg-press") return { kind: "plate_loaded_machine", baseIncrement: 20, rampStyle: "plate_jump" }; if (exercise.equipment === "machine") return { kind: "selectorised_machine", baseIncrement: exercise.stackIncrement ?? 5, rampStyle: "small_step" }; return { kind: "selectorised_machine", baseIncrement: exercise.stackIncrement ?? 2.5, rampStyle: "small_step" }; }
export function magnitudeLoad(exercise: Exercise, weight: number, reps: number, targetHigh: number, rir?: number) { const profile = progressionProfile(exercise); if ((rir ?? 2) < 3 || reps < targetHigh) return nextPracticalLoad(weight, exercise, "up"); const excess = Math.max(0, reps - targetHigh); const jumps = profile.kind === "dumbbell" ? Math.min(3, 1 + Math.floor(excess / 4)) : profile.kind === "plate_loaded_machine" ? Math.min(2, 1 + Math.floor(excess / 5)) : 1; let result = weight; for (let i = 0; i < jumps; i++) result = nextPracticalLoad(result, exercise, "up"); return result; }
export function rampLoad(exercise: Exercise, current: number, target: number) { const profile = progressionProfile(exercise); if (profile.rampStyle === "plate_jump") return Math.min(target, current + Math.max(profile.baseIncrement, Math.round(current * .15 / profile.baseIncrement) * profile.baseIncrement)); return Math.min(target, nextPracticalLoad(current, exercise, "up")); }
