import type { SupplierCatalogueProduct, SupplierCatalogueStore, ClubSupplier } from "./club-supplier-catalogue";
export type GsnImportRow = { brand:string; range:string; name:string; category?:string; subcategory?:string; description?:string; size?:string; retailPriceMinor?:number; sourceUrl?:string; parentImageReference?:string; variantImageReference?:string; nutrition?:Record<string,unknown>; ingredients?:string; allergens?:string; active?:boolean };
export function normaliseGsnRows(rows:GsnImportRow[]): GsnImportRow[] { return rows.filter(r=>r.active!==false&&r.name.trim()).map(r=>({...r,brand:r.brand.trim(),range:r.range.trim(),name:r.name.trim()})); }
export function groupGsnCatalogue(rows:GsnImportRow[], supplier:ClubSupplier): SupplierCatalogueProduct[] { const map=new Map<string,SupplierCatalogueProduct>();for(const r of normaliseGsnRows(rows)){const key=`${r.brand.toLowerCase()}|${r.range.toLowerCase()}`;const p=map.get(key)??{parentKey:key,supplierId:supplier.id,brand:r.brand,name:r.range,category:r.category,variants:[]};p.variants.push({supplierId:supplier.id,parentKey:key,flavour:r.name,size:r.size,stockStatus:"unknown"});map.set(key,p);}return [...map.values()]; }
export function gsnMemberOrderable(){return false;}
