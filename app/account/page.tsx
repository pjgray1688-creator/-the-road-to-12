"use client";
import { useState } from "react";
import { browserSupabase } from "@/lib/supabase-browser";
export default function AccountPage() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const submit = async (mode: "signIn" | "signUp") => { const supabase = browserSupabase(); const result = mode === "signIn" ? await supabase.auth.signInWithPassword({ email, password }) : await supabase.auth.signUp({ email, password }); setMessage(result.error?.message ?? (mode === "signUp" ? "Check your email to confirm your account." : "Signed in.")); }; return <main className="legal-page account-page"><header><span className="brand-mark">T/12</span><div><p className="eyebrow">THE ROAD TO 12%</p><h1>Account</h1></div></header><p>Create an account or sign in to keep your training data private and synced.</p><label>Email<input type="email" value={email} onChange={event => setEmail(event.target.value)} /></label><label>Password<input type="password" value={password} onChange={event => setPassword(event.target.value)} /></label><div className="account-actions"><button className="primary" onClick={() => submit("signIn")}>Sign in</button><button className="secondary" onClick={() => submit("signUp")}>Create account</button></div>{message && <p>{message}</p>}<p className="muted">Password reset and sign-out are provided by the configured Supabase Auth project.</p></main>; }
