import {redirect} from "next/navigation";import{serverSupabase}from"@/lib/supabase-server";import{resolveClubOrganisationContext}from"@/lib/club-server-context";import{AppNav}from"@/components/app-nav";import{AppShell,EmptyState,PageHeader,Surface}from"@/components/ui";import{ClubSectionNav}from"@/components/club-shell";import{ClubCatalogueImport}from"@/components/club-catalogue-import";import{ClubSupplierCatalogue}from"@/components/club-supplier-catalogue";import{ClubCatalogueOperations}from"@/components/club-catalogue-operations";
export default async function SupplierCataloguePage({searchParams}:{searchParams?:Promise<{org?:string}>}){const c=await serverSupabase();const{data:{user}}=await c.auth.getUser();if(!user)redirect("/account?mode=signIn");const x=await resolveClubOrganisationContext(c,user.id,(await searchParams)?.org);if(!x||!(await x.repository.hasCapability(x.organisation.id,user.id,"supplier.catalogue_manage")))return <AppShell><PageHeader title="Supplier catalogue"/><EmptyState title="Access required">Ask an owner.</EmptyState><AppNav/></AppShell>;const[{data},products]=await Promise.all([c.rpc("club_list_supplier_catalogue",{p_organisation_id:x.organisation.id}),x.repository.listCommerceProducts(x.organisation.id)]);const offers=Array.isArray(data)?data:[];return <AppShell className="module-page club-page"><PageHeader title="Supplier catalogue"/><ClubSectionNav organisation={x.organisation} role={x.role} contexts={x.availableContexts}/><ClubSupplierCatalogue organisationId={x.organisation.id} offers={offers as Array<Record<string,unknown>>} products={products}/><details><summary>Catalogue operations</summary><Surface><ClubCatalogueOperations organisationId={x.organisation.id} products={products.map(p=>({id:p.id,name:p.name,brand:p.brand,sku:p.sku,barcode:p.barcode,sellPriceMinor:p.sellPriceMinor,stockTracked:p.stockTracked}))} balances={[]}/><ClubCatalogueImport organisationId={x.organisation.id} products={products.map(p=>({id:p.id,name:p.name}))}/></Surface></details><AppNav/></AppShell>}
