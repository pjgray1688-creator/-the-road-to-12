import { redirect } from "next/navigation";
import { AppNav } from "@/components/app-nav";
import { ClubSectionNav } from "@/components/club-shell";
import { AppShell, EmptyState, PageHeader } from "@/components/ui";
import { resolveClubOrganisationContext } from "@/lib/club-server-context";
import { serverSupabase } from "@/lib/supabase-server";
import { ClubPromotions } from "@/components/club-promotions";
export default async function ClubPromotionsPage({searchParams}:{searchParams?:Promise<{org?:string}>}) { const client=await serverSupabase(); const {data:{user}}=await client.auth.getUser(); if(!user) redirect("/account?mode=signIn"); const context=await resolveClubOrganisationContext(client,user.id,(await searchParams)?.org); if(!context || !(await context.repository.hasCapability(context.organisation.id,user.id,"commerce.pricing_manage"))) return <AppShell className="module-page club-page"><PageHeader eyebrow="R12 CLUB" title="Promotions & offers"/><EmptyState title="Promotion access required">Only authorised Club managers can create offers.</EmptyState><AppNav/></AppShell>; const {data}=await client.from("club_promotions").select("id,name,description,status,starts_at,ends_at,location_ids,eligibility,effects").eq("organisation_id",context.organisation.id).order("starts_at",{ascending:false}); return <AppShell className="module-page club-page"><PageHeader eyebrow="R12 CLUB · SHOP" title="Promotions & offers" description="Manage live, scheduled and past customer offers."/><ClubSectionNav organisation={context.organisation} role={context.role} contexts={context.availableContexts}/><ClubPromotions organisationId={context.organisation.id} promotions={Array.isArray(data)?data:[]}/><AppNav/></AppShell>; }
