import { NextRequest, NextResponse } from "next/server";
import { consumeState, exchangeCode } from "@/lib/whoop-server";
export async function GET(request: NextRequest) { const url = new URL(request.url); const state = url.searchParams.get("state") ?? ""; const code = url.searchParams.get("code"); if (!consumeState(state) || state !== request.cookies.get("whoop_oauth_state")?.value) return NextResponse.json({ error: "Invalid OAuth state" }, { status: 400 }); if (!code) return NextResponse.json({ error: url.searchParams.get("error") ?? "Authorization was not completed" }, { status: 400 }); try { await exchangeCode(code); return NextResponse.redirect(new URL("/", request.url)); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "WHOOP connection failed" }, { status: 502 }); } }
