import { NextRequest, NextResponse } from "next/server";
import { serverSupabase } from "@/lib/supabase-server";
import { consumeState, exchangeCode } from "@/lib/whoop-server";
export async function GET(request: NextRequest) { const url = new URL(request.url); const state = url.searchParams.get("state") ?? ""; const code = url.searchParams.get("code"); const supabase = await serverSupabase(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Authentication required" }, { status: 401 }); if (!consumeState(state) || state !== request.cookies.get("whoop_oauth_state")?.value) return NextResponse.json({ error: "Invalid OAuth state" }, { status: 400 }); if (!code) return NextResponse.json({ error: url.searchParams.get("error") ?? "Authorization was not completed" }, { status: 400 }); try { await exchangeCode(code, user.id); return NextResponse.redirect(new URL("/", request.url)); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "WHOOP connection failed" }, { status: 502 }); } }
