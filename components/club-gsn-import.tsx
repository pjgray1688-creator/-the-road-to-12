"use client";import{useState}from"react";import{parseGsnCsv}from"@/lib/gsn-catalogue";export function ClubGsnImport(){const[p,setP]=useState<any>();return <section aria-label="Import GSN local catalogue"><h3>Import GSN local catalogue</h3><p className="hint">Local stock only; supplier ordering disabled. Physical stock is unchanged.</p><input type="file" accept=".csv" onChange={e=>{const f=e.target.files?.[0];if(f)void f.text().then(t=>setP(parseGsnCsv(t)))}}/>{p?<p className="hint">{p.rows.length} accepted · {p.errors.length} excluded/invalid rows · preview only</p>:null}</section>}
