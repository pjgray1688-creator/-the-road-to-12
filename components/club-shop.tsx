"use client";
import { useState, useTransition } from "react";
import type { ClubBalanceAccount, ClubCommerceProduct, ClubOrder } from "@/lib/club-commerce";
import type { ClubCashDeclaration } from "@/lib/club-commercial-rules";
import { purchaseAction, reconcileCashAction } from "@/app/club/shop/actions";
import styles from "./club-shop.module.css";

const money = (minor: number) => `£${(minor / 100).toFixed(2)}`;
const stateLabel = (status: string) => ({ pending_payment: "Payment needed", awaiting_cash_verification: "Awaiting cash verification", cash_disputed: "Cash needs review", paid: "Paid", fulfilled: "Completed", cancelled: "Cancelled", refunded: "Refunded" } as Record<string,string>)[status] ?? status;

export function ClubShop({ organisationId, userId, products, locations, balance, orders, declarations, staff, accent }: { organisationId: string; userId: string; products: ClubCommerceProduct[]; locations: { id: string; name: string; active: boolean }[]; balance?: ClubBalanceAccount; orders: ClubOrder[]; declarations: ClubCashDeclaration[]; staff: boolean; accent: string }) {
  const [product, setProduct] = useState<ClubCommerceProduct>(); const [quantity, setQuantity] = useState(1); const [locationId, setLocationId] = useState(locations.find(item => item.active)?.id ?? ""); const [payment, setPayment] = useState<"balance"|"cash_box">("balance"); const [message, setMessage] = useState<string>(); const [pending, startTransition] = useTransition();
  const submit = () => { if (!product) return; setMessage(undefined); startTransition(async () => { const result = await purchaseAction({ productId: product.id, locationId: locationId || undefined, quantity, payment }); setMessage(result.ok ? (result.status === "paid" ? "Paid from Madhouse Balance." : "Awaiting cash verification. Place the cash in the selected location cash box.") : result.error); if (result.ok) setProduct(undefined); }); };
  const reconcile = (declarationId: string, status: "confirmed"|"rejected"|"discrepancy") => startTransition(async () => { const result = await reconcileCashAction({ declarationId, status }); setMessage(result.ok ? "Cash declaration updated." : result.error); });
  return <div className={styles.shop} style={{ "--club-accent": accent } as React.CSSProperties}><div className={styles.balance}><span>MADHOUSE BALANCE</span><strong>{balance ? money(balance.balanceMinor) : "Not set up"}</strong></div><section><div className={styles.sectionHead}><div><span className="eyebrow">PRODUCTS</span><h2>Available at Club</h2></div></div>{products.length ? <div className={styles.productGrid}>{products.map(item => <button className={styles.product} key={item.id} type="button" onClick={() => { setProduct(item); setQuantity(1); setMessage(undefined); }}><span className={styles.productCategory}>{item.category ?? "Club"}</span><strong>{item.name}</strong><span>{money(item.sellPriceMinor)}</span></button>)}</div> : <div className={styles.empty}>No products are currently available.</div>}</section>{orders.length ? <section><div className={styles.sectionHead}><div><span className="eyebrow">YOUR ORDERS</span><h2>Recent purchases</h2></div></div><div className={styles.orders}>{orders.slice(0,8).map(order => <div className={styles.order} key={order.id}><span>{new Date(order.createdAt).toLocaleDateString("en-GB")}</span><strong>{money(order.totalMinor)}</strong><em>{stateLabel(order.status)}</em></div>)}</div></section> : null}{staff ? <section><div className={styles.sectionHead}><div><span className="eyebrow">RECEPTION</span><h2>Cash to verify</h2></div></div>{declarations.length ? <div className={styles.orders}>{declarations.map(item => <div className={styles.declaration} key={item.id}><div><strong>{money(item.declaredAmountMinor)}</strong><span>{item.locationId ? locations.find(location => location.id === item.locationId)?.name : "Location not set"} · {new Date(item.declaredAt).toLocaleString("en-GB")}</span></div><div className={styles.actions}><button type="button" onClick={() => reconcile(item.id, "confirmed")} disabled={pending}>Confirm</button><button type="button" onClick={() => reconcile(item.id, "discrepancy")} disabled={pending}>Discrepancy</button><button type="button" onClick={() => reconcile(item.id, "rejected")} disabled={pending}>Reject</button></div></div>)}</div> : <div className={styles.empty}>No cash declarations waiting.</div>}</section> : null}{message ? <p className={styles.message} role="status">{message}</p> : null}{product ? <div className="modal" role="dialog" aria-modal="true"><div className={styles.sheet}><div className={styles.sheetHead}><div><span className="eyebrow">PURCHASE</span><h2>{product.name}</h2></div><button type="button" onClick={() => setProduct(undefined)}>Close</button></div><p className={styles.price}>{money(product.sellPriceMinor * quantity)}</p><label>Quantity<input type="number" min={1} max={20} value={quantity} onChange={event => setQuantity(Math.max(1, Number(event.target.value) || 1))} /></label><label>Payment<select value={payment} onChange={event => setPayment(event.target.value as typeof payment)}><option value="balance">Madhouse Balance</option><option value="cash_box">Cash box</option><option disabled value="card">Card / online payment coming shortly</option></select></label>{payment === "cash_box" ? <><label>Location<select value={locationId} onChange={event => setLocationId(event.target.value)}>{locations.filter(item => item.active).map(location => <option value={location.id} key={location.id}>{location.name}</option>)}</select></label><p className={styles.hint}>Place the cash in the selected location cash box; staff will verify it.</p></> : null}<button type="button" className="primary full" onClick={submit} disabled={pending || payment === "cash_box" && !locationId}>{pending ? "Processing…" : "Place order"}</button></div></div> : null}</div>;
}
